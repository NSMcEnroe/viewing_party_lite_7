<h1>New User</h1>


<%= form_with model: @user, url: users_path(@user), method: :post, local: true, data: { turbo: false } do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :email %>
  <%= f.text_field :email %>

  <%= f.submit "Register" %>
<% end %>
